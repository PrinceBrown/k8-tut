#!/bin/sh

# Example: Print the start message
echo "Starting the Node.js application..."

# Execute the command provided as arguments to the script
# This will typically be `npm start` or similar
exec "$@"
